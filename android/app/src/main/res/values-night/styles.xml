<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
    <!-- * Le agrego el color primario del archivo colors (este es el splash)-->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- <item name="android:windowSplashScreenBackground">@mipmap/ic_launcher_</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@mipmap/ic_launcher_</item> -->

        <item name="android:windowFullscreen">false</item>
        <item name="android:forceDarkAllowed">false</item>

        <!-- <item name="android:windowBackground">@drawable/launch_background</item> -->
        <!-- <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item> -->
        <!-- <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowLightStatusBar">true</item> -->

              <!-- <item name="android:windowBackground">@drawable/launch_background</item> -->
      <!-- <item name="android:windowDrawsSystemBarBackgrounds">true</item> -->
      <!-- <item name="android:statusBarColor">@android:color/transparent</item> -->
      <!-- <item name="android:windowLightStatusBar">false</item> -->
      <!-- <item name="android:forceDarkAllowed">false</item>
      <item name="android:windowFullscreen">true</item> -->
      <!-- <item name="android:navigationBarColor">@color/primary</item>
      <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item> -->
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
    <!-- TODO: se debe de poner el color que se va utilizar de fondo en la app -->    
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>
